import packgeInfo from '../../package.json'

export const APP_NAME = packgeInfo?.name || 'uni-app-template'
export const APP_DESCRIPTION = packgeInfo?.description || ''
export const APP_VERSION = packgeInfo?.version
export const APP_ID = ''
export const APP_ID_WECHAT = ''
